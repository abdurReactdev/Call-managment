const baseUrl = 'https://frontend-test-api.aircall.io/';
export const getData = () => {
  // axios.get(`${baseUrl}GET /calls`);
};

export const login = (username, password) => {
  console.log(password);
  // axios.post('');
};
